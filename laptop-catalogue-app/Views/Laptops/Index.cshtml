    @model IEnumerable<PROG2231_Assignment2__9031031_.Models.Laptop>
    @{
        ViewData["Title"] = "Laptop Catalogue";
    }


    <!DOCTYPE html>

    <html>
    <head>
        <meta name="viewport" content="width=device-width" />
    <title>@ViewData["Title"]</title>
</head>
<body>

<div class="text-center mb-4">
    <h1 class="display-6 fw-semibold">@ViewData["Title"]</h1>
    <a asp-action="Create" asp-controller="Laptops" class="btn btn-dark py-2 mt-3">Add New Laptop 💻</a>
</div>
    <div class="text-center">
    <table class="table table-hover">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.Model)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Brand)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Year)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.Model)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Brand)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Year)
            </td>
            <td>
                <a asp-action="Edit" asp-route-id="@item.LaptopId" class="btn btn-outline-warning btn-table rounded shadow-sm">Edit</a> 
                <a asp-action="Details" asp-route-id="@item.LaptopId" class="btn btn-outline-warning btn-table rounded shadow-sm">Details</a> 
                <a asp-action="Delete" asp-route-id="@item.LaptopId" class="btn btn-outline-warning btn-table rounded shadow-sm">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
</div>
</body>
</html>
